# ============================================================
# 官方插件功能开关配置
# ============================================================
# 规则: 设置为 1 表示启用，0 表示禁用
# 注意: 仅影响用户态插件，确保内核哈希一致性
# ============================================================

# 基础插件
ENABLE_ARGON=1
ENABLE_DISKMAN=1
ENABLE_IRQBALANCE=1
ENABLE_FILEBROWSER_GO=1
ENABLE_SQM=1
ENABLE_TTYD=1
ENABLE_AUTOREBOOT=1
ENABLE_SMARTDNS=1
ENABLE_RAMFREE=1

# 容器与高级功能
ENABLE_DOCKER=0

# 第三方 APK 插件 (这些在 third-party.config 中定义)
# 注意：这里只控制是否注入到 .config，具体的 APK 安装逻辑由 apk-prepare-packages.sh 处理
